// GET /api/profile?url=... — public endpoint, orchestrates the pipeline:
// validate -> rate limit -> session -> fetch -> parse -> respond.
// Milestone 5 (endpoint) + milestone 6 (rate limiting).
export {};
