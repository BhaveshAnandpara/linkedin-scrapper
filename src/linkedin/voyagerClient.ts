// Low-level HTTP client for LinkedIn's Voyager API: builds headers/cookies
// from a Session, calls fetch(), classifies the response into a typed
// result or transport error. Milestone 2.
export {};
